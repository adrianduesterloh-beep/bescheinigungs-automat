/**
 * Die Versionsnummer des Programms. Wird in der Oberfläche, unter /health
 * und im User-Agent gegenüber Ablefy und dem Mailanbieter gezeigt.
 *
 * Bei jeder Änderung, die an den Empfänger geht: hier hochzählen und den
 * Eintrag in CHANGELOG.md schreiben. Die Oberfläche zeigt die Nummer unten
 * auf jeder Seite — so sieht der Betreiber nach einem Update sofort, ob die
 * neue Fassung schon läuft.
 */
export const VERSION = '1.2.0';
export const PROGRAMM = 'bescheinigungs-automat';
